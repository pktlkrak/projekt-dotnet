# ClinicManager - a simple dotnet application for managing a clinic.

## CI

The CI's job is to test the application every time a new commit gets merged, whether it be by pushing a commit onto a branch or by merging a pull request.

It first checks if the application builds correctly, then runs all the tests in the ClinicManagerTests subproject.
